#!/usr/bin/python2.7
'''

create settings and scripts to run esorex:

esorex --config=esorex.cfg --recipe-config=51Peg_UVESredchain.cfg recipe sofSettings.sof

'''
__author__ = 'jmartins'

# ======================================================================================================================
#   Python Modules
# ======================================================================================================================
import os


# ======================================================================================================================
#   User Modules
# ======================================================================================================================


# ======================================================================================================================
#   Settings
# ======================================================================================================================

def fnCreateFolder(path):
    """create_folder(path) - function that will create the necessary folder
        structure if it does not exist
        path - path of folder to be created
    """
    if not os.path.isdir(path):
        os.mkdir(path)
